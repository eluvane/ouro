{
  "kind": "ouro.kernel-budgets.v2",
  "timing_domain": "canonical-retained-executable-process-wall.v1",
  "retained_execution": {
    "min_fixtures": 17,
    "max_unexpected_fixtures": 0,
    "max_elapsed_ms": 500.0,
    "description": "Every exact retained Ouro law must pass within the execution budget in all three builds. Compilation is measured separately."
  },
  "orchestration": {
    "workers": 1,
    "memory_limit_mib": 3072,
    "check_timeout_s": 900,
    "build_timeout_s": 600,
    "run_timeout_s": 120,
    "boundary_timeout_s": 120
  },
  "native_tool_cache": {
    "required_modes": ["off", "miss", "hit"],
    "description": "Compare actual uncached, fresh cached and cache-restored executables. This is build-cache evidence; the retired semantic memo toggle is absent."
  },
  "compiler_boundary": {
    "required_checks": ["compiler-checker-dependencies", "compiler-checked-value-owners", "compiler-boundary-documentation"],
    "max_issues": 0
  },
  "legacy_baseline": "Python JSON replay elapsed_ms and memo cache timings are obsolete and cannot be used as this executable baseline."
}
