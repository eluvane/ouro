name: Pages

on:
  push:
    branches:
      - main
      - master
    paths:
      - site/**
      - quality/biome.json
      - .github/workflows/ouro-pages.yml

permissions:
  contents: read

concurrency:
  group: ouro-pages-${{ github.ref }}
  cancel-in-progress: true

defaults:
  run:
    shell: bash

jobs:
  build-site:
    name: Build
    runs-on: ubuntu-latest
    timeout-minutes: 15
    permissions:
      contents: read
    steps:
      - name: Checkout
        uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1 # v7.0.1
        with:
          persist-credentials: false

      - name: Restore npm cache
        id: site-npm-cache
        uses: actions/cache/restore@55cc8345863c7cc4c66a329aec7e433d2d1c52a9 # v6.1.0
        with:
          path: ~/.npm
          key: ouro-site-npm-${{ runner.os }}-${{ hashFiles('site/package-lock.json') }}
          restore-keys: |
            ouro-site-npm-${{ runner.os }}-

      - name: Build site
        run: |
          set -euo pipefail
          node --version
          npm --version
          mkdir -p _build/site
          cd site
          npm ci --prefer-offline --no-audit --no-fund
          npm run lint
          npm run build
          cd ..
          printf 'site_build=ok\n' > _build/site/build.txt

      - name: Save npm cache
        if: ${{ github.event_name == 'push' && (github.ref == 'refs/heads/main' || github.ref == 'refs/heads/master') && steps.site-npm-cache.outputs.cache-hit != 'true' }}
        uses: actions/cache/save@55cc8345863c7cc4c66a329aec7e433d2d1c52a9 # v6.1.0
        with:
          path: ~/.npm
          key: ${{ steps.site-npm-cache.outputs.cache-primary-key }}

      - name: Upload Pages artifact
        uses: actions/upload-pages-artifact@fc324d3547104276b827a68afc52ff2a11cc49c9 # v5.0.0
        with:
          path: site/dist

      - name: Upload reports
        if: always()
        uses: actions/upload-artifact@043fb46d1a93c77aae656e7c1c64a875d1fc6a0a # v7.0.1
        with:
          name: pages-build
          if-no-files-found: ignore
          retention-days: 14
          path: |
            _build/**/*.json
            _build/**/*.log
            _build/**/*.txt
            _build/**/*.md

  publish-pages:
    name: Publish
    runs-on: ubuntu-latest
    needs: build-site
    if: ${{ github.event_name == 'push' && github.ref == 'refs/heads/main' }}
    timeout-minutes: 15
    permissions:
      pages: write
      id-token: write
      contents: read
    environment:
      name: github-pages
      url: ${{ steps.deployment.outputs.page_url }}
    steps:
      - name: Deploy Pages
        id: deployment
        uses: actions/deploy-pages@368f82528645a54fb793d4d04e342629a3f51346 # v5.0.1
