-- Runtime helper lookup keys are ordinary definition names. Keep the
-- spelling next to the byte identity the lowering contract consumes.
def managed_runtime_symbol (name : String) : List Nat :=
    let codes : List Nat := prim_string_to_char_codes name in codes;
