-- Source parser composition over canonical expression and declaration owners.
-- The file parser receives `parse` explicitly, as it does at the bootstrap ABI.

import "parser_parse.ouro";
import "parser_file.ouro";
