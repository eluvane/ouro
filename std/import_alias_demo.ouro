-- Import alias demos must stay inside syntax accepted by the packaged frontend.

import "record_demo.ouro" as Rec;

def demo_origin : Rec.Point := Rec.origin;
def demo_x : Nat := open Rec in get_x origin;
