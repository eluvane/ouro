inductive Nat : Type := | Z : Nat | S : Nat -> Nat;
inductive Vec (A : Type) : Nat -> Type :=
  | vnil : Vec A Z
  | vcons : (n : Nat) -> A -> Vec A n -> Vec A (S n);
def empty_nat_vec : Vec Nat Z := vnil Nat;
def singleton (x : Nat) : Vec Nat (S Z) := vcons Nat Z x (vnil Nat);
