-- Tutorial 05: structural recursion (match + decreasing arg)
-- Next: 06_effects.ouro — pure algebraic-effect smoke (Ask -> "Ouro").
-- @entry pred
inductive Nat : Type := | Z : Nat | S : Nat -> Nat;

def pred (n : Nat) : Nat :=
    match n with
    | Z => Z
    | S n' => n'
    end;
