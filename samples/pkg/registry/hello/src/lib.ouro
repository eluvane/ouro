-- Package "hello": a leaf package, no dependencies.

-- @entry hello_id hello_msg

inductive Greeting : Type :=
  | Hello : Greeting;

def hello_id (A : Type) (x : A) : A := x;

def hello_msg : Greeting := Hello;
