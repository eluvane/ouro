-- Pure-only handler: wrap a do let! program; the pure clause is the result.
import "../../std/io.ouro";

-- @entry main
def main : IO Unit :=
  handle
    do let! line := readLine;
        println (concat "handler=" line);
        exit 0
  with
  | pure r => r
  end
