-- Do-notation bind: name a value from an IO action, then use it.
import "../../std/io.ouro";

-- @entry main
def main : IO Unit :=
  do let! line := readLine;
      println (concat "line=" line);
      exit 0
