-- Stdin echo: read one line, print it back with a prefix.
import "../../std/io.ouro";

-- @entry main
def main : IO Unit :=
  do let! line := readLine;
      println (concat "echo=" line);
      exit 0
