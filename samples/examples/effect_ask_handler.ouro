-- Bounded operation-handler smoke: arity-0 Ask, one-shot resume.

import "../../std/runtime.ouro";

-- @entry main
effect Ask : String

def main : String :=
  handle perform Ask with
  | Ask k => k "Ouro"
  | pure x => x
  end
