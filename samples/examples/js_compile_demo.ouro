-- Pure JS compiler pipeline sample (no dump-core).
-- @entry ttrue ffalse idB
inductive Bool : Type := | BB : Bool | BT : Bool;

def ttrue : Bool := BT;
def ffalse : Bool := BB;
def idB (b : Bool) : Bool := b;
