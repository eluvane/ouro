-- Bounded IO do-bind smoke: bind one stdin line and print it.

import "../../std/runtime.ouro";

-- @entry main
def main : IO Unit :=
  do let! line := prim_stdin_read_line;
      prim_stdout_write (prim_string_concat("line=", prim_string_concat line "\n"));
      prim_exit 0
