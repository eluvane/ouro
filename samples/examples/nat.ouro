-- Nat and structural recursion.

-- @entry four
inductive Nat : Type :=
  | Z : Nat
  | S : Nat -> Nat;

def add : Nat -> Nat -> Nat :=
  fix add (n : Nat) (m : Nat) : Nat :=
    match n with
    | Z => m
    | S n' => S (add n' m)
    end;

def two : Nat := 2;

def four : Nat := add two two;
