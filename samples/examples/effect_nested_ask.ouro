-- Nested handlers: nearest-wins (inner Ask resumes to "inner").

import "../../std/runtime.ouro";

-- @entry main
effect Ask : String

def main : String :=
  handle
    handle perform Ask with
    | Ask k => k "inner"
    | pure x => x
    end
  with
  | Ask k => k "outer"
  | pure x => x
  end
