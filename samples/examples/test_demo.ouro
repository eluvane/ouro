-- Smallest user test: std/test helpers plus ouro1 test.

import "../../std/test.ouro";

def cases : List Test :=
  [ assert_eq "add" (prim_string_of_nat (add (S (S Z)) (S Z))) "3"
  , assert_true "nonzero" (notb (eq_nat Z (S Z)))
  ];

-- @entry main
def main : IO Unit :=
    test_run cases
