-- Thin keep-import surface for intern/path helpers. lexer.ouro stays
-- skip-harvested unless this file is the selected root.
import "../../compiler/lexer.ouro";
import "../../compiler/path.ouro";
import "../../compiler/pipeline_abi.ouro";

def load_intern_string (intern : InternState) (s : List Nat)
    : Pair Nat InternState :=
    intern_string intern s;

def load_canon_path (base : List Nat) (rel : List Nat) : List Nat :=
    canon_path base rel;
