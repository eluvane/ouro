-- Structural proof record only. scan.ouro keeps the protocol parser so a
-- selected consumer can name CgSProof without keep-harvesting that cone.
import "../../std/string_prims.ouro";

inductive CgSProof : Type :=
  | MkCgSProof : String -> String -> Nat -> String -> CgSProof;
