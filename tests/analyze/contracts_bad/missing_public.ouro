-- The unit opts in: every public definition needs a requires or ensures.
-- @property contracts

inductive Nat : Type :=
  | Z : Nat
  | S : Nat -> Nat;

-- @entry main
def main (n : Nat) : Nat := n;

-- Private definitions are exempt.
def helper (n : Nat) : Nat := n;
