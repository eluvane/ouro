inductive Nat : Type :=
  | Z : Nat
  | S : Nat -> Nat;

def add (a : Nat) (b : Nat) : Nat := a;

-- 256 is one past the byte/ASCII limit; the frontend hosts the decimal.
-- @entry main
def main (n : Nat) : Nat := add n 256;
