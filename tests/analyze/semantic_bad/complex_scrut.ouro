inductive Bool : Type :=
  | True : Bool
  | False : Bool;

-- @entry main
def main (a : Bool) (b : Bool) : Bool :=
  match (match a with | True => b | False => a end) with
  | True => False
  | False => True
  end;
