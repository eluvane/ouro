inductive Nat : Type :=
  | Z : Nat
  | S : Nat -> Nat;

-- A lowercase constructor is read as a binder in pattern slots.
inductive Shape : Type :=
  | circle : Shape
  | Square : Shape;

-- @entry main
def main (s : Shape) : Nat := Z;
