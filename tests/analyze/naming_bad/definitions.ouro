inductive Nat : Type :=
  | Z : Nat
  | S : Nat -> Nat;

-- One character is not a searchable name.
def f (n : Nat) : Nat := n;

-- A capital initial on a value definition reads as a constructor.
def Twice (n : Nat) : Nat := S (S n);

-- @entry main
def main (n : Nat) : Nat := Twice (f n);
