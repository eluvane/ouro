-- Pipeline good: both constructors are referenced from the entry.
-- @entry main

inductive Color : Type :=
  | Red : Color
  | Blue : Color;

def paint : Color -> Color :=
  fun (c : Color) =>
    match c with
    | Red => Blue
    | Blue => Red
    end;

def main : Color := paint Red;
