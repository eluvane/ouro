-- Pipeline good: similar syntax but distinct normalized bodies, plus small trivial bodies.

def tiny_a : Nat := Z;
def tiny_b : Nat := Z;
def nonclone_a : Nat := add (add (add (add (add Z Z) Z) Z) Z) Z;
def nonclone_b : Nat := add (add (add (add (add (S Z) Z) Z) Z) Z) Z;
