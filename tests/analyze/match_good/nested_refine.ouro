-- Two arms for one constructor are distinct when a slot names a constructor.
inductive Flag : Type :=
  | On : Flag
  | Off : Flag;

inductive Box : Type :=
  | MkBox : Nat -> Flag -> Box;

def unbox (b : Box) : Nat :=
  match b with
  | MkBox n On => n
  | MkBox _ Off => Z
  end;
