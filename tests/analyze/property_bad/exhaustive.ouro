inductive Bool : Type :=
  | True : Bool
  | False : Bool;

inductive Nat : Type :=
  | Z : Nat
  | S : Nat -> Nat;

-- @property exhaustive
def missing (n : Nat) : Bool := match n with | Z => True end;

-- @property exhaustive
-- @entry main
def main (b : Bool) : Bool := match b with end;
