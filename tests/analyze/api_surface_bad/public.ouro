-- Pipeline bad: one unexpected export, one signature drift, one export the
-- baseline marks internal-only, and the first export of a duplicated name.

def new_api : Nat := Z; -- public
def drift_api (x : Nat) : Nat := x; -- public
def internal_api : Nat := Z; -- public
def dup_api : Nat := Z; -- public
