-- Pipeline bad: dead_private is not reachable from main; dead_export is
-- exported but nothing reaches it, and an export is a promise, not a root.
-- @entry main
-- @export dead_export

def helper : Nat := Z;
def main : Nat := helper;
def dead_private : Nat := Z;
def dead_export : Nat := Z;
