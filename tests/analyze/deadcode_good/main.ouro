-- Pipeline good: private helper is reachable; the exported helper is reached
-- from the entry; public API root has no internal callers by design.
-- @entry main
-- @export helper

def helper : Nat := Z;
def main : Nat := helper;
def planned_public : Nat := Z; -- public
