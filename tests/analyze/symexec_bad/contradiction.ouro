inductive Bool : Type :=
  | True : Bool
  | False : Bool;

-- Inside the True arm no arm of the inner match can fire.
-- @entry main
def main (b : Bool) : Bool :=
  match b with
  | True => match b with | False => False end
  | False => False
  end;
