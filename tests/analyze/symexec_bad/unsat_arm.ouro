inductive Bool : Type :=
  | True : Bool
  | False : Bool;

-- Inside the True arm the inner False arm contradicts the path condition.
-- @entry main
def main (b : Bool) : Bool :=
  match b with
  | True =>
      match b with
      | True => True
      | False => False
      end
  | False => False
  end;
