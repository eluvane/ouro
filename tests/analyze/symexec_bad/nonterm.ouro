inductive Nat : Type :=
  | Z : Nat
  | S : Nat -> Nat;

-- The recursive call repeats every parameter unchanged.
-- @entry main
def main : Nat -> Nat -> Nat :=
  fix spin (n : Nat) (acc : Nat) : Nat :=
    match n with
    | Z => acc
    | S _ => spin n acc
    end;
