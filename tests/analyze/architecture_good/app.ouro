-- Pipeline good: outer application layer imports a lower shared layer.
-- ouro-analyze:layer=3 std-runtime=false trusted=false compiler-internal=false
import "std_base.ouro";

def app_entry : Nat := std_value;
