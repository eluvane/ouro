inductive Nat : Type :=
  | Z : Nat
  | S : Nat -> Nat;

-- The scrutinee is definitely zero, so the S arm can never fire.
-- @entry main
def main : Nat :=
  let count : Nat := Z in
  match count with
  | Z => Z
  | S k => k
  end;
