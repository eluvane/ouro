inductive Nat : Type :=
  | Z : Nat
  | S : Nat -> Nat;

def div_nat (a : Nat) (b : Nat) : Nat := a;

-- @entry main
def main : Nat := div_nat (S Z) Z;

def via_let : Nat := let divisor : Nat := Z in div_nat (S Z) divisor;
