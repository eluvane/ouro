-- Typed-fact smoke: one export, one entry, one ctor, one live branch.
-- @entry sample_main
-- @export sample_main

inductive Bit : Type :=
  | B0 : Bit
  | B1 : Bit;

def sample_helper : Bit := B0;

def sample_main : Bit :=
  match sample_helper with
  | B0 => B1
  | B1 => B0
  end;
