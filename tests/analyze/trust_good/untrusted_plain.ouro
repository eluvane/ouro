-- ouro-analyze:layer=3 trusted=false
-- ouro-analyze:source=raw_input
-- Effects and taint sources outside the trusted path are not trust facts.
effect Log where
  | write : Nat -> Nat

def raw_input : Nat := Z;

def user : Nat := perform write

def reads : Nat := raw_input;
