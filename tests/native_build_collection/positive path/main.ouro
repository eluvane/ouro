-- Both imports reach the same leaf; the collector must include it once.
-- The compiler accepts newlines and an omitted final semicolon here.
import
  "nested lib.ouro"; import "shared/leaf.ouro"

def main : Runtime W := nested_answer;
