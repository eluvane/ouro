-- An absent/invalid stdin handle is irrelevant to a zero-byte action.
import "source_stdio_values.ouro";

def main : IO Unit :=
    do let! value := prim_stdin_read_bytes 0;
       match prim_string_length value with | Z => print "zero"
       | S _ => exit 1 end
