import "source_directory_values.ouro";

def main : IO Unit :=
    do let! names := fs_list "directories/many";
       directory_expect (eq_nat(length String names, 128));
       directory_expect (all String (fun (name : String) => prim_string_starts name "item-") names);
       print (str_join "\n" names);
       exit 42
