import "source_startup_values.ouro";

intrinsic chosen_mutation : String -> IO Unit := "ouro.fs.remove";

def main : IO Unit :=
    let result : IO Unit := mutation_typed_reused chosen_mutation in result;
