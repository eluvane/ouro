-- One retained action must query the clock on each execution.
import "source_stdio_values.ouro";

def main : IO Unit :=
    let action : IO String := now in
    do let! first := action;
       println first;
       let! second := action;
       print second
