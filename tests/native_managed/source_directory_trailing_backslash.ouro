import "source_directory_values.ouro";

def main : IO Unit :=
    do let! names := fs_list "directories/empty\\";
       directory_expect (eq_nat (length String names) 0);
       let! readable := prim_fs_listable "directories/empty\\";
       directory_expect readable;
       exit 42
