import "source_string_values.ouro";

def source_literal (zero : U32) : U32 :=
    source_expect_string "native" (source_identity String "native") zero;

def main : Runtime U32 := runtime_bind U32 U32 source_zero
    (fun (zero : U32) => runtime_pure(U32, source_literal zero));
