import "source_loop_values.ouro";

def main : Runtime U32 :=
    runtime_bind U32 U32 source_zero (fun (zero : U32) =>
      let pending : Runtime U32 := runtime_loop U32 zero loop_abort_condition loop_abort in
      runtime_pure U32 (source_forty_two zero));
