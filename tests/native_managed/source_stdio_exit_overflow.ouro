import "source_stdio_values.ouro";

def main : IO Unit :=
    runtime_bind U32 Unit source_zero (fun (zero : U32) =>
      match u32_from_nat 1 with | Nothing => exit 73 | Just one =>
        let maximum : Nat := stdio_nat_of_u32 (u32_sub zero one) in
        exit (S maximum) end);
