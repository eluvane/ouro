import "source_startup_values.ouro";

def main : IO Unit :=
    let operation : String -> IO Unit := prim_fs_mkdir in
    let unused_nul : IO Unit := operation (prim_string_of_char_codes [65, 0, 66]) in
    let unused_utf8 : IO Unit := operation (prim_string_of_char_codes [255]) in
    exit 42
