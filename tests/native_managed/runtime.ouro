-- Real checked/imported Ouro runtime input for the MIR ABI fixtures. The
-- fixture driver retains these compiled functions and adds a separate entry.
import "../../runtime/managed.ouro";
import "source_values.ouro";

def main : Runtime U32 :=
    let result : Runtime U32 := source_zero in result;
