import "source_fs_mutation_values.ouro";

def main : IO Unit :=
    do let! arguments := argv;
       match arguments with | Nil => exit 91 | Cons _ paths =>
         do mutation_paths prim_fs_remove paths; exit 42 end
