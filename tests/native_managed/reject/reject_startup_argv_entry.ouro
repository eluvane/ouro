import "../source_values.ouro";

intrinsic String : Type := "ouro.string";
intrinsic selected : Runtime (List String) := "ouro.runtime.argv";

def main : Runtime Unit := runtime_bind (List String) Unit selected (fun (_ : List String) => runtime_pure Unit MkUnit);
