import "../source_values.ouro";

intrinsic String : Type := "ouro.string";
intrinsic selected : String -> Runtime (List String) := "ouro.fs.list_dir";

def main : Runtime Unit := runtime_bind (List String) Unit (selected "directories/unused") (fun (_ : (List String)) => runtime_pure Unit MkUnit);
