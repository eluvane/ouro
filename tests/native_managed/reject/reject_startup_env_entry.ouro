import "../source_values.ouro";

intrinsic String : Type := "ouro.string";
intrinsic selected : String -> Runtime (Maybe String) := "ouro.runtime.env_get";

def main : Runtime Unit := runtime_bind (Maybe String) Unit (selected "OURO_STARTUP_TEST_MISSING") (fun (_ : Maybe String) => runtime_pure Unit MkUnit);
