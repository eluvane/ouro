-- Native input and output preserve every byte, including NUL and high bytes.
import "source_stdio_values.ouro";

def main : IO Unit :=
    do let! first := prim_stdin_read_bytes 8;
       print first;
       let! tail := prim_stdin_read_bytes 8;
       print tail
