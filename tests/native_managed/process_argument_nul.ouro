-- NUL cannot be represented in Windows argv; it must not truncate to success.
import "../../std/io.ouro";

def main : IO Unit :=
    do let! result := prim_process_capture "unused" [prim_string_of_char_codes [65,0,66]];
       exit 91
