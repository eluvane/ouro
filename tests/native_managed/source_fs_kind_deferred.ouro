import "source_fs_kind_values.ouro";

def main : IO Unit :=
    let unused : String -> IO Nat := prim_fs_kind in
    let invalid : IO Nat := unused (prim_string_of_char_codes [255]) in
    let embedded_zero : IO Nat := unused (prim_string_of_char_codes [65, 0, 66]) in
    exit 42;
