import "source_startup_values.ouro";

def main : IO Unit :=
    do let! value := env_get "OURO_STARTUP_TEST_VALUE";
       startup_expect (match value with | Nothing => False | Just text => prim_string_eq text "abc" end);
       exit 42
