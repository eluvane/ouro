import "../../std/io.ouro";
import "source_values.ouro";

def main : IO Unit :=
    do let! count := io_pure Nat 41;
       let! value := io_pure Nat (S count);
       match u32_from_nat value with
       | Nothing => io_unit
       | Just result => windows_exit_process result
       end
