import "source_directory_values.ouro";

def main : IO Unit :=
    do let! missing := prim_fs_listable "directories/missing";
       let! file := prim_fs_listable "directories/file.txt";
       directory_expect (andb (notb missing) (notb file));
       exit 42
