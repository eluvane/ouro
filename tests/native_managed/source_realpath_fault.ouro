import "source_startup_values.ouro";

def main : IO Unit :=
    do let! result := prim_fs_realpath (prim_string_concat "realpaths/" "file");
       startup_expect (orb (prim_string_eq result "") (prim_string_ends result "/realpaths/file"));
       exit 42
