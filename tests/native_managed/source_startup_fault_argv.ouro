import "source_startup_values.ouro";

def main : IO Unit :=
    do let! values := argv;
       startup_expect (match values with | Nil => False | Cons first rest =>
         andb (prim_string_eq first "a") (match rest with | Nil => True | Cons _ _ => False end) end);
       exit 42
