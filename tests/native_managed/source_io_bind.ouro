import "../../std/io.ouro";
import "source_values.ouro";

def main : IO Unit :=
    io_bind Nat Unit (io_pure Nat 42) (fun (value : Nat) =>
      match u32_from_nat value with
      | Nothing => io_unit
      | Just result => windows_exit_process result
      end);
