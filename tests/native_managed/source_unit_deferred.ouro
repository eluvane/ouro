import "source_values.ouro";

def unit_unused (zero : U32) : Runtime Unit :=
    let pending : Runtime Unit := windows_exit_process (source_forty_two zero) in
    runtime_pure Unit MkUnit;
def main : Runtime Unit := runtime_bind U32 Unit source_zero unit_unused;
