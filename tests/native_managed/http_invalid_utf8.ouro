-- Native loader must observe exit 73, never a request or a fake response.
import "http_values.ouro";

def main : IO Unit :=
    do let! result := prim_http_request (prim_string_of_char_codes [255]) [] "body";
       exit 91;
