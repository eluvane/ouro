import "source_nat_values.ouro";
import "source_values.ouro";

def main : Runtime U32 :=
    runtime_bind U32 U32 source_zero (fun (zero : U32) =>
      let expected : U32 := source_forty_two zero in
      runtime_pure U32 (source_answer zero (nat_u32_same expected (nat_u32_to_nat expected))));
