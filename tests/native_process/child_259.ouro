import "../../runtime/platform/windows_x64.ouro";
def main : Runtime U32 :=
    match u32_from_nat 259 with | Just code => runtime_pure U32 code
    | Nothing => runtime_bind USize U32 (raw_to_bits(U8, raw_null U8))
        (fun (zero : USize) => runtime_pure(U32, raw_size_to_u32 zero)) end;
