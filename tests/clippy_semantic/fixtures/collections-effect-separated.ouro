import "../../../std/runtime.ouro";
def collection_example (xs : List Bool) : IO Nat :=
    let ys : List Bool := map Bool Bool notb xs in
    (do let! _ := prim_stdout_write ".";
        io_pure Nat (length Bool ys));
