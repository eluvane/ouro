-- Semantic precision fixture: branch-separated-normalization
import "../../../std/string.ouro";
def normalize_branch (flag : Bool) (raw : String) : String := match flag with | True => str_trim raw | False => str_trim raw end;
