import "../../../std/fs.ouro";

def observe_nested_read (path : String) : IO Unit :=
    do let! result := (do let! inner := fs_read_checked path;
                         io_pure (Either FsError String) inner);
       match result with
       | Left error => io_pure Unit MkUnit
       | Right text => io_pure Unit MkUnit
       end
