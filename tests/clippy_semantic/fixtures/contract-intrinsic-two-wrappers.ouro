-- Full worker contract mutation fixture; real intrinsic identity.
import "../../../std/string_prims.ouro";
-- ouro-structural: {"rule":"STRUCT_PASS_THROUGH_WRAPPER","category":"independent-oracle","related":["tests/clippy_semantic/fixtures/contract-intrinsic-two-wrappers.ouro#delegated"],"reason":"This negative semantic-contract fixture must retain the exact wrapper graph to test transitive contract propagation; removing its wrappers would erase the exercised case."}
def normalized (text : String) (old : String) (new : String) : String := prim_string_replace text old new;
def delegated (text : String) (old : String) (new : String) : String := normalized text old new;
def twice (input : String) : String := prim_string_concat (delegated input "a" "b") (delegated input "a" "b");
