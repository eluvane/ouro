-- Intrinsic identity mutation; no IO/platform import cone.
import "../../../std/string_prims.ouro";
def branch (flag : Bool) (s : String) : String := match flag with | True => prim_string_replace s "a" "b" | False => prim_string_replace s "a" "b" end;
