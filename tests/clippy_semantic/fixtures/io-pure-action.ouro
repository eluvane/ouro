import "../../../std/io.ouro";

def wrapped_action_difference (task : IO Nat) : IO Nat :=
    do let! action := io_pure (IO Nat) task;
       let! first := action;
       let! second := action;
       io_pure Nat (sub second first)
