import "../../../std/listx.ouro";
def collection_example (flag : Bool) (xs : List Bool) : Nat :=
    let ys : List Bool := map Bool Bool notb xs in
    match flag with | True => length Bool ys | False => 0 end;
