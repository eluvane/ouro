-- Contract precision fixture: contract-checked-two-wrappers
import "../../../std/fs.ouro";
-- ouro-structural: {"rule":"STRUCT_WRAPPER_CHAIN","category":"independent-oracle","related":["std/fs.ouro#fs_read_checked","tests/clippy_semantic/fixtures/contract-checked-two-wrappers.ouro#forward_read"],"reason":"This negative semantic-contract fixture must retain the exact wrapper graph to test transitive contract propagation; removing its wrappers would erase the exercised case."}
def bridge_read (path : String) : IO (Either FsError String) := fs_read_checked path;
def forward_read (renamed : String) : IO (Either FsError String) := bridge_read renamed;
def discard_bridge (path : String) : IO Unit :=
    do let! value := forward_read path; io_pure Unit MkUnit
