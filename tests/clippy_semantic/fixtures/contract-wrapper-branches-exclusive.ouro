-- Contract precision fixture: contract-wrapper-branches-exclusive
import "../../../std/string.ouro";
def normalize_bridge (text : String) : String := str_trim text;
def exclusive_wrapper (flag : Bool) (text : String) : String :=
    match flag with | True => normalize_bridge text | False => normalize_bridge text end;
