import "../../../std/fs.ouro";

def discard_second_statement (path : String) : IO Unit :=
    let action := fs_read_checked path in
    do let! first := action;
       match first with
       | Left error => io_pure Unit MkUnit
       | Right text => do action; io_pure Unit MkUnit
       end
