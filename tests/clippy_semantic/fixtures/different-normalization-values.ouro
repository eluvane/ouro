-- Semantic precision fixture: different-normalization-values
import "../../../std/string.ouro";
def normalize_pair (left : String) (right : String) : String := prim_string_concat (str_trim left) (str_trim right);
