-- Contract precision fixture: contract-cheap-wrapper-no-performance
import "../../../std/string_prims.ouro";
def cheap_bridge (text : String) : String := text;
def repeated_cheap (text : String) : String :=
    prim_string_concat (cheap_bridge text) (cheap_bridge text);
