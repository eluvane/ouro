-- For this callback pair only original True values survive, as False.
import "../../../std/listx.ouro";
def collection_example (xs : List Bool) : List Bool :=
    foldr Bool (List Bool) (fun (b : Bool) (acc : List Bool) =>
      match b with | True => Cons Bool False acc | False => acc end) [] xs;
