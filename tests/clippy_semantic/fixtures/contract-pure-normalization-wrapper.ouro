-- Contract precision fixture: contract-pure-normalization-wrapper
import "../../../std/string.ouro";
def normalize_bridge (text : String) : String := str_trim text;
def repeated_wrapper (text : String) : String :=
    prim_string_concat (normalize_bridge text) (normalize_bridge text);
