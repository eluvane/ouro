import "../../../std/fs.ouro";

def wrap_without_reading (path : String) : IO Unit :=
    let action := fs_read_checked path in
    do let! alias := io_pure (IO (Either FsError String)) action;
       io_pure Unit MkUnit
