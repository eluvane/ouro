-- Full worker contract mutation fixture; real intrinsic identity.
import "../../../std/string_prims.ouro";
def duplicated (text : String) (old : String) (new : String) : String := prim_string_replace text old old;
def twice (input : String) : String := prim_string_concat (duplicated input "a" "b") (duplicated input "a" "b");
