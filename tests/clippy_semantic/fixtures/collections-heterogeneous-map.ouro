-- Only the intermediate String identity must agree, not all map types.
import "../../../std/string.ouro";
def collection_example (xs : List String) : List Nat :=
    map String Nat str_len (map String String str_lower xs);
