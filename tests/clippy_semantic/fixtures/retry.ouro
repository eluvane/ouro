-- Semantic precision fixture: retry
import "../../../std/fs.ouro";
def retry_read (first : String) (second : String) : IO (Either FsError String) := do let! result := fs_read_checked first; match result with | Left error => fs_read_checked second | Right text => io_pure (Either FsError String) (Right FsError String text) end
