-- Full worker contract mutation fixture; real intrinsic identity.
import "../../../std/string_prims.ouro";
def reordered (text : String) (old : String) (new : String) : String := prim_string_replace text new old;
def twice (input : String) : String := prim_string_concat (reordered input "a" "b") (reordered input "a" "b");
