-- An alias retains the actual registered declaration identity.
import "../../../std/listx.ouro";
def collection_example (xs : List Bool) : Nat :=
    let mapper : Bool -> Bool := notb in
    length Bool (map Bool Bool mapper xs);
