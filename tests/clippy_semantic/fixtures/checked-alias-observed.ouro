-- Semantic precision fixture: checked-alias-observed
import "../../../std/fs.ouro";
def observe_alias (path : String) : IO Unit := do let! result := fs_read_checked path; let alias := result in match alias with | Left error => io_pure Unit MkUnit | Right text => io_pure Unit MkUnit end
