-- Semantic precision fixture: checked-alias-discard
import "../../../std/fs.ouro";
def discard_alias (path : String) : IO Unit := do let! result := fs_read_checked path; let alias := result in io_pure Unit MkUnit
