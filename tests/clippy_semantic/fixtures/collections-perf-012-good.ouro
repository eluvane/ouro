-- Select False and map each selected value to True in one traversal.
import "../../../std/listx.ouro";
def collection_example (xs : List Bool) : List Bool :=
    foldr Bool (List Bool) (fun (b : Bool) (acc : List Bool) =>
      match b with | False => Cons Bool True acc | True => acc end) [] xs;
