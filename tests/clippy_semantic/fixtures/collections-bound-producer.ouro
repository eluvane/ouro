-- A bound producer may have other consumers; do not claim its allocation is waste.
import "../../../std/listx.ouro";
def collection_example (xs : List Bool) : Nat :=
    let ys : List Bool := map Bool Bool notb xs in
    let alias : List Bool := ys in
    length Bool alias;
