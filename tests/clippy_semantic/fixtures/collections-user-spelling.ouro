-- Same spellings are deliberately NOT the std/string declarations.
import "../../../std/string_prims.ouro";
def str_trim (text : String) : String := prim_string_concat "[" text;
def str_ltrim (text : String) : String := prim_string_concat text "]";
def collection_example (text : String) : String := str_trim (str_ltrim text);
