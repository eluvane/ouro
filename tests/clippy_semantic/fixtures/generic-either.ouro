-- Semantic precision fixture: generic-either
import "../../../std/types.ouro";
def sum_adapter (value : Either Nat Nat) : Either Nat Nat := match value with | Left n => Right Nat Nat n | Right n => Right Nat Nat n end;
