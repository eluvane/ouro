import "../../../std/fs.ouro";

def discard_second_read (path : String) : IO Unit :=
    let action := fs_read_checked path in
    do let! first := action;
       match first with
       | Left error => io_pure Unit MkUnit
       | Right text =>
           do let! second := action;
              io_pure Unit MkUnit
       end
