import "../../../std/fs.ouro";

def observe_pure_alias (path : String) : IO Unit :=
    do let! result := fs_read_checked path;
       let! alias := io_pure (Either FsError String) result;
       match alias with
       | Left error => io_pure Unit MkUnit
       | Right text => io_pure Unit MkUnit
       end
