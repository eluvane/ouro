-- Export only the pure typed-MIR probe alongside the ordinary source driver.
-- Development hosts transport values; Ouro owns checking, MIR and native code.
import "native_managed_source_driver.ouro",
  "native_copy_fixtures.ouro";
